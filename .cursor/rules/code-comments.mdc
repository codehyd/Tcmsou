---
description: 写任何功能或修改时必须加生活化注释；注释与代码分行，并按格式化文档后的样式编写
alwaysApply: true
---

# 代码注释规范（Agent 写代码必须遵守）

做任何功能、修改、修复时都要加注释。先写清「这段代码干什么、解决什么麻烦」，再用生活里常见的事打比方；需要时再补一句代码层面的意思。不要把注释写成代码的逐字翻译。

注释用中文。写完后必须是「格式化文档」之后的样子：缩进、空行、换行一次到位，不要先堆成一团再指望格式化去救。

## 格式（严格）

- 注释独占一行，写在对应代码**正上方**。禁止行尾注释（`代码 // 注释`）。
- 注释和代码必须分行，中间不要夹别的语句。
- 不同「注释 + 代码」小组之间空一行，方便扫读。
- 函数、组件、文件开头的说明，与函数体/实现之间空一行。
- 不要为纯括号、单独的 `return` 符号、import 清单逐条注废话。

```ts
// ❌ 禁止：注释贴在代码后面，读起来像一锅粥
const [open, setOpen] = useState(false) // 控制弹窗

// ❌ 禁止：只重复代码，没有目的和生活例子
// 声明 open 状态初始值为 false
const [open, setOpen] = useState(false)

// ✅ 正确：上一行注释，下一行代码；讲目的 + 好懂的比方，必要时带一点代码意思
// 记着弹窗开没开，像房间灯的开关；false 就是默认关着（useState）
const [open, setOpen] = useState(false)
```

## 变量 / 状态 / 配置

每一处新状态、关键变量、配置，都要在上方说明：它记住什么、谁在用、不记会怎样。

```tsx
// 记下用户正在搜的药名，像药店柜台上的纸条，后面过滤列表靠它
const [keyword, setKeyword] = useState('')

// 药材详情接口地址，换环境只改这里，避免满项目找字符串
const HERB_API = '/api/herbs'
```

## 方法 / 函数（每一段相关逻辑都要注）

函数开头先用几行说清：谁来叫它、它帮人完成什么、没它会卡在哪。函数体里**每一段相关逻辑**（判断、取值、转换、请求、写入、返回）都要在该行代码上方单独注释，方便以后按注释查。

```ts
// 按药名找药材：用户在搜索框敲字时调用
// 没有它，列表就不知道该留下谁、藏起谁，像超市广播找不到货架
function findHerbByName(list: Herb[], name: string) {
  // 没打字就当没搜，把原清单原样递回去，免得空搜把货架搬空
  if (!name.trim()) {
    return list
  }

  // 忽略大小写和前后空格，避免「陈皮」和「 陈皮 」被当成两种药
  const needle = name.trim().toLowerCase()

  // 只留下名字里带关键字的，相当于按纸条去货架上拣
  return list.filter((item) => item.name.toLowerCase().includes(needle))
}
```

## 修改已有代码时

改逻辑就改（或补）对应注释，别让注释还在说旧故事。删代码时把失效注释一起删掉。

## 自检

- 外行人看注释，能懂这是在解决什么问题吗？
- 注释和代码是否分行、小组之间是否空行，看起来像已经格式化过？
- 函数里每段关键逻辑是否都能靠上方注释定位？
